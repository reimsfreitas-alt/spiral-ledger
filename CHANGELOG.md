# Changelog — Spiral Ledger
## 1.0.0 · 2026-07
Primeiro release comercial. AOF durável (fsync) com corrente SHA-256; leitura O(1); assinatura Ed25519 (não-repúdio opcional-exigível); API v1 (records, latest, chain/verify, export, health); SDK embutido+HTTP; bind local por padrão; gate de token para exposição em rede; lock de escritor único por AOF; selo `npm run verify` (11 provas).
